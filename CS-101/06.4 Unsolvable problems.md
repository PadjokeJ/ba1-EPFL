#aicc 

Not every problem can be solved by an algorithm. 

Turing designed the **halting problem**, which is unsolvable

We have a computer and an input. Can we determine wether the problem will halt after a while with only that input? 

No we can't.

