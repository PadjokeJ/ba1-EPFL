#analyse 

>[!théorème]
>Si $g(x)\neq 0$ et $g'(x)\neq 0$, et que la limite $\lim_{x\to a}f(x)/g(x)$ est $0\over 0$ ou $\pm\infty\over\pm\infty$, alors 
>$$\lim_{x\to a} {f(x)\over g(x)} = \lim_{x\to a}{f'(x)\over g'(x)}$$

On peut même utiliser la règle de BH plusieurs fois