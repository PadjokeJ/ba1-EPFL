#aicc
The truth value of P($x$) depends on the value of $x$  
When working with [[02.3 Predicate logic|predicates]],  we have to define a domain $U$ (universe) where the value of $x$ can be sourced from.

### Example : 

Q($x$) := "$x$ is greater than 3"  
$U$ : $\mathbb{Z}$
