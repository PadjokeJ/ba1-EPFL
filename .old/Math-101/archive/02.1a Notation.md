#analyse #archive
Mostly for convenience, but used for mathematic notation

$\forall$ = for any/all
$\not \forall$ = for none (useless)
$\exists$ = there exists
$\not \exists$ = there isn't any
$\exists!$ = there exists one (unique)

